// Input of some text  tp html site 

function makeHeader (t)
{
    document.write("<h4>"+t+"</h4");
    document.write("it is working");
}
function loop (f)
{
    for (var k=1; k <= f ; k++)
    {  
        // create variable
        var txt = makeHeader ();
    }
}

